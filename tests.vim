let g:before = [ 'ggdG0',
                \'Inp.mean(thing(), arg1)',
                \'gg0']

let g:tests = [  '$hdaw',
                \'dsF']

let g:after = [ '' ]

let g:expected = [ 'np.mean(thing(), )',
                  \'thing()']


