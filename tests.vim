let g:before = [ 'I'
                \'np.mean(thing(), arg1)',
                \'gg0' ]

let g:tests = [  '$hdaw',
                \'dsF' ]

let g:after = [ '' ]

let g:expected = [ 'np.mean(thing(), )',
                  \'thing()' ]


